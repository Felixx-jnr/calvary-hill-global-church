/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "calvaryhillglobalchurch.s3.eu-north-1.amazonaws.com",
      "images.app.goo.gl",
    ],
  },
};

export default nextConfig;
